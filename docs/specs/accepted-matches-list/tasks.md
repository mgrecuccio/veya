# Accepted Matches List Tasks

Status: Planned

- [ ] Confirm the accepted matches DTO used by `MatchesService.getAccepted()`.
- [ ] Replace static match preview data in `matches.page.ts`.
- [ ] Load accepted matches through `MatchesService.getAccepted()`.
- [ ] Add a view model for display name, initials/fallback label, channel, status, overlap, and timestamps.
- [ ] Add loading state.
- [ ] Add empty state.
- [ ] Add error state.
- [ ] Update `matches.page.html` to render backend data and states.
- [ ] Update styles for new states without changing unrelated page layout.
- [ ] Add focused page tests for success, empty, error, and retry/refresh behavior if implemented.
